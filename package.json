{
  "name": "pokemon-set-collect",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first Pokemon card collection tracker.",
  "scripts": {
    "build": "vite build",
    "start": "npm run build && node server.js",
    "server": "node server.js",
    "dev": "vite --host 127.0.0.1",
    "check": "node --check server.js && vite build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
